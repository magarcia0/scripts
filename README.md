# A collection of simple python scripts that do the following:
**filter.py** - tokenizes each item by white-space, removes comma's, and seperates each string by a newline. 

**sort.py** - sorts strings alphabetically
